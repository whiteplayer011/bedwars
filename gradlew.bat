@IF NOT "%DEBUG%"=="" @ECHO OFF
@SETLOCAL

@rem Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to this script.
@rem Add -Dorg.gradle.jvmargs=-Xmx1024m if you wish to override the default JVM options.
set DEFAULT_JVM_OPTS=

set DIRNAME=%~dp0
if "%DIRNAME%"=="" set DIRNAME=.

@rem ... (restante do conteúdo do script gradlew.bat padrão do Gradle)
